// SPDX-License-Identifier: MIT
pragma solidity >=0.4.22 <0.9.0;

import "@openzeppelin/contracts/utils/math/SafeMath.sol";

// Helper library for gene computations
library GeneScience {
  using SafeMath for uint256;
	
	// TODO: fill 
	function mergeSwords(uint _firstParentDna, uint _secondParentDna) public pure returns (uint) {
		uint randomGene = 1;
		return randomGene;
	}

	// TODO: fill
	function calculateAttackPowerFromDna(uint _dna) public pure returns(uint) {
		uint attackPower = 1;
		return attackPower;
	}

	function generateBasicDna() public pure returns (uint) {
		uint basicGene = 0;
		return basicGene;
	}
}